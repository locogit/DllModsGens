/// <summary>
/// The main entry point for the injection.
/// </summary>
extern "C" _declspec(dllexport) void Init()
{
	MessageBox(nullptr, "how many times do i need to tell yo stupid ass to not use this mod", "Unleashed UI", MB_ICONERROR);
	MessageBox(nullptr, "i put an obsolete notice up and everything and yo ass can not read", "Unleashed UI", MB_ICONERROR);
	MessageBox(nullptr, "i need you to stop using this mod there is no reason to fucking dipshit", "Unleashed UI", MB_ICONERROR);
	exit(-1);
}